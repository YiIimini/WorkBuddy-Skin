# 2026-07-30 工作日志

## WorkBuddy-Skin v2.4 修复 + GSAP 美化 + 发布

**修复（WorkBuddySkin.swift）**
- 窗口点 "x" 后无法重开：`isReleasedWhenClosed=false` + 实现 `applicationShouldHandleReopen`（didBecomeActive 在 app 已激活时不触发）
- 菜单栏点击闪退：`updateMenuStatus`/`refreshStatus` 后台线程读 Swift Dictionary `currentConfig` 与主线程写竞争 → 改为主线程快照三段式
- 菜单"启用背景"错绑 `autoTextToggled` → 改 `toggleBackground`

**GSAP 美化（daemon.js 注入脚本）**
- 按钮磁吸悬浮/按压回弹（事件委托 + overwrite:auto）
- 弹窗弹簧进场 + 子项 stagger（MutationObserver + rAF 批处理）
- 输入框焦点光晕呼吸 + caret 主题色 + ::selection 高亮
- CSS 过渡剔除 transform（交给 GSAP），支持 prefers-reduced-motion 降级
- daemon 新增 `/vendor/gsap.min.js` 路由（gsap@3.15 装在托管 node workspace）

**发布**
- 版本 bump v2.4（build-app.sh / Swift 标题 / README / RELEASE.md）
- 编译 → DMG（dist/WorkBuddy-Skin-v2.4.dmg）→ commit + tag v2.4 + push → gh release 上传 DMG
- 同步 daemon.js 到部署目录 `~/WorkBuddy-Skin/` 并重启守护进程（CDP 已重连，GSAP 路由 200）

**知识库**：新增 `项目开发/WorkBuddy-Skin v2.4 更新记录.md`，与 `WorkBuddy-Skin 项目.md` 双向链接

**技能沉淀**：`~/.workbuddy/skills/macos-menubar-app-pitfalls/`

## 版本号更正（v2.4 → v1.0.0）
- 用户确认当前版本应为 **v1.0.0**（项目版本线以 GitHub Release 为准，历史 tag v2.0~v2.3 无对应 Release，勿按 tag 递增）
- 已删除 v2.4 Release + tag（gh release delete --cleanup-tag）
- 全部版本号回滚：Swift 标题 / build-app.sh / README / RELEASE.md → v1.0.0
- 重新编译打包 `dist/WorkBuddy-Skin-v1.0.0.dmg`，tag v1.0.0 强制移至 fb93ff8 并推送
- 已有 v1.0.0 Release 用 --clobber 替换 DMG 并更新 notes，设为 Latest
- 知识库笔记重命名为 `WorkBuddy-Skin v1.0.0 更新记录.md`，双向链接已同步

## 输入框深度美化（commit 16ee231）
- 聊天容器 `.atm-modal-chat-input` 光影 CSS 变量化：`--wb-glow`（聚焦光环）+ `--wb-type`（打字辉光），多层 box-shadow 用 `calc(var()*系数)` 驱动
- GSAP：focusin 容器 y:-3 + `--wb-glow:1`；focusout 回落；input 事件 `gsap.set(--wb-type:0.85)` 后 0.9s 衰减；容器首次出现上浮进场（800ms 轮询，30s 上限）
- 新增 placeholder / ProseMirror `is-editor-empty` 占位提示主题色
- 关键模式：**GSAP 可直接 tween CSS 变量**（`gsap.to(el, {'--wb-glow': 1})`），光影走 CSS calc，避开 GSAP 无法操作伪元素/复杂阴影串插值的限制
- 已同步 ~/WorkBuddy-Skin 并重启 daemon（CDP 重连，gsap 路由 200）

## 关键事实
- 部署目录 `~/WorkBuddy-Skin/` 是独立副本（非软链），daemon.js 改动需手动 cp 同步；config.json 以部署目录为准，不要覆盖
- 托管 node: `/Users/x/.workbuddy/binaries/node/versions/22.22.2/bin/node`
